import type { AnalyticsRecord, Output, ReferenceRecord, Request, Review, Settings } from '../types';
const now='2026-05-22';
export let requests:Request[] = Array.from({length:8}).map((_,i)=>({request_id:`REQ-20260522-00000${i}`,request_title:`طلب محتوى ${i+1}`,created_at:now,created_by:'AZ',source:'Google Sheet',request_type:i%3===0?'reel':'carousel',platform:['instagram','snapchat','linkedin'][i%3],raw_input:'فكرة خام',extra_context:'سياق',priority:['high','medium','low'][i%3],mode:'build_series',assigned_ai:'CLAUDE',review_required:i%2===0,status:['NEW','CLAUDE_IN_PROGRESS','WAITING_USER_REVIEW','READY_FOR_FINAL','READY_FOR_RECORDING','READY_FOR_PUBLISH','DONE','FAILED'][i],needs_visual:true,content_goal:'توعية',tone:'متوازن',needs_user_action:i%2===0,action_owner:'AZ',required_action:'مراجعة أو تصوير',recording_notes:'نبرة هادئة',due_date:now,review_owner:'AZ',user_rejected:i===2,rejected_by_user:i===2,rejection_reason:i===2?'لا يناسب':'',revision_source:'Claude',updated_at:now}));
export let outputs:Output[]=[1,2,3].map(i=>({output_id:`OUT-${i}`,request_id:requests[i].request_id,created_at:now,claude_result:'نتيجة Claude',decision_card:'بطاقة قرار',content_pillar:'معرفة',series:'سلسلة عز',chosen_angle:'زاوية تنفيذية',output_type:i===1?'reel':'single_image',platform:'instagram',final_copy:'نص نهائي جاهز للنشر',visual_brief:'تصور بصري',selected_design_id:`AZ-DESIGN-00${i}`,design_prompt:'prompt',content_quality_score:8+i%2,visual_consistency_notes:'متسق',status:i===1?'READY_FOR_FINAL':'READY_FOR_PUBLISH',publish_ready:true,final_asset_url:'https://example.com/file',design_preview_url:'https://example.com/preview',caption:'كابشن',content_format:'نصي',publish_notes:'جاهز'}));
export let reviews:Review[]=[1,2].map(i=>({review_id:`REV-${i}`,request_id:requests[i].request_id,output_id:outputs[0].output_id,reviewer:'ChatGPT',audit_status:'accepted',severity:'medium',score:7+i,what_worked:'هوك ممتاز',what_failed:'يحتاج اختصار',correction_order:'اختصر المقدمة',final_recommendation:'جاهز بعد تعديل بسيط',created_at:now}));
export let references:ReferenceRecord[]=[1,2,3].map(i=>({reference_id:`REF-${i}`,type:['أعجبني','ريلز','تصميم'][i-1],url:'https://example.com',notes:'ملاحظة',reference_reason:'مرجع أسلوب',related_design_id:`AZ-DESIGN-00${i}`}));
export let analytics:AnalyticsRecord[]=[1,2].map(i=>({post_id:`POST-${i}`,post_url:'https://example.com/post',platform:'instagram',publish_date:now,content_type:'reel',pillar:'توعية',series:'سلسلة',views:1000*i,reach:800*i,saves:20,shares:10,comments:12,replies:5,completion_rate:65,profile_visits:100,clicks:40,average_watch_time:18,replays:30,screenshots:3,exits:15,notes:'جيد'}));
export let settings:Settings={sheet_endpoint_url:'',api_token:'',use_mock_data:true,default_mode:'build_series',polling_seconds:30,sheet_mapping:{Dashboard:'Dashboard',Setup:'Setup',Requests:'Requests',Outputs:'Outputs',Reviews:'Reviews',Logs:'Logs',Analytics:'Analytics',Settings:'Settings',References:'References',Lists:'Lists'}};
